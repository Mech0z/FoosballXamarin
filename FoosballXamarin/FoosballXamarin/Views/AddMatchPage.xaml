<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FoosballXamarin.Views.AddMatchPage"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:FoosballXamarin.ViewModels;assembly=FoosballXamarin.UWP"
             d:DataContext="{d:DesignInstance {x:Type vm:AddMatchViewModel}}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Title="Add Match"
             mc:Ignorable="d">
    <ContentPage.Content>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="8*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="8*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackLayout
                Grid.Column="0">
                <Label
                    FontSize="18"
                    HorizontalOptions="Center"
                    Text="{Binding User1.Username}" />
                <Label
                    FontSize="18"
                    HorizontalOptions="Center"
                    Text="{Binding User2.Username}" />
            </StackLayout>
            <Label
                Grid.Column="1"
                FontSize="18"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                Text="VS" />
            <StackLayout
                Grid.Column="2">
                <Label
                    FontSize="18"
                    HorizontalOptions="Center"
                    Text="{Binding User3.Username}" />
                <Label
                    FontSize="18"
                    HorizontalOptions="Center"
                    Text="{Binding User4.Username}" />
            </StackLayout>

            <StackLayout 
                Grid.Row="1" 
                Orientation="Horizontal" 
                HorizontalOptions="Center"
                Grid.ColumnSpan="3">
                <Entry
                    Keyboard="Numeric"
                    Text="{Binding ScoreTeam1Match1}" />
                <Entry
                    Keyboard="Numeric"
                    Text="{Binding ScoreTeam2Match1}"/>
            </StackLayout>
            <StackLayout 
                Grid.Row="2" 
                Orientation="Horizontal" 
                HorizontalOptions="Center"
                Grid.ColumnSpan="3">
                
                <Entry
                    Keyboard="Numeric"
                    Text="{Binding ScoreTeam1Match1}" />
                <Entry
                    Keyboard="Numeric"
                    Text="{Binding ScoreTeam2Match1}"/>
            </StackLayout>
            <Button
                Grid.Row="3"
                Grid.ColumnSpan="3"
                HorizontalOptions="Center"
                Text="Submit"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>